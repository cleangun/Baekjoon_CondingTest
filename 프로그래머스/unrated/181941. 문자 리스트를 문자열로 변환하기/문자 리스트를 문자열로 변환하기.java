import java.util.Arrays;

class Solution {
    public String solution(String[] arr) {
        String answer = "";
        for (int idx = 0; idx < arr.length; idx++){
            answer += arr[idx];
        }
        return answer;
    }
}